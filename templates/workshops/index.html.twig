{% extends 'base.html.twig' %}

{% block title %}Workshops index{% endblock %}

{% block body %}
    <h1>Workshops index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Schedule</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for workshop in workshops %}
            <tr>
                <td>{{ workshop.id }}</td>
                <td>{{ workshop.name }}</td>
                <td>{{ workshop.schedule }}</td>
                <td>
                    <a href="{{ path('app_workshops_show', {'id': workshop.id}) }}">show</a>
                    <a href="{{ path('app_workshops_edit', {'id': workshop.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_workshops_new') }}">Create new</a>
{% endblock %}
