{% extends 'base.html.twig' %}

{% block title %}Workshops{% endblock %}

{% block body %}
    <h1>Workshops</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ workshop.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ workshop.name }}</td>
            </tr>
            <tr>
                <th>Schedule</th>
                <td>{{ workshop.schedule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_workshops_index') }}">back to list</a>

    <a href="{{ path('app_workshops_edit', {'id': workshop.id}) }}">edit</a>

    {{ include('workshops/_delete_form.html.twig') }}
{% endblock %}
